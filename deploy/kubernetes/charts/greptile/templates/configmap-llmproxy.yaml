{{- if .Values.components.llmproxy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "greptile.llmproxyConfigName" . }}
  labels:
    {{- include "greptile.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Files.Get "files/llmproxy-config.yaml" | indent 4 }}
{{- end }}
