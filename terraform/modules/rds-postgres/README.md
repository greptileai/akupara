# RDS PostgreSQL Module

Manages a PostgreSQL RDS instance plus its subnet group and security group. Designed for on-prem Greptile deployments that need a managed database with opinionated defaults but overridable sizing and security parameters.
